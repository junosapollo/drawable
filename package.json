{
  "name": "drawable",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@drawable/web",
    "dev:api": "node scripts/run-python.mjs services/api/.venv -m linescout_api.main",
    "dev:all": "node scripts/dev-all.mjs",
    "build": "npm run build --workspace=@drawable/web",
    "check": "npm run check --workspace=@drawable/contracts && npm run check --workspace=@drawable/web",
    "test": "npm run test --workspace=@drawable/web",
    "test:e2e": "npm run test:e2e --workspace=@drawable/web",
    "contracts": "node scripts/run-python.mjs services/api/.venv scripts/export_openapi.py && npm run generate --workspace=@drawable/contracts",
    "setup:py": "node scripts/setup-python.mjs",
    "check:py": "node scripts/check-python.mjs",
    "smoke": "node scripts/run-python.mjs services/api/.venv scripts/smoke_search.py"
  },
  "engines": {
    "node": ">=22"
  }
}
